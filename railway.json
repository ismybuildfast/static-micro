{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "./build.sh"
  },
  "deploy": {
    "startCommand": "npx serve public -l 3000",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
